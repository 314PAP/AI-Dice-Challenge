/**
 * SEZNAM POUŽÍVANÝCH CSS TŘÍD:
 * Bootstrap: container-fluid, vh-100, overflow-hidden, row, col-12, col-md-8, col-md-4, h-100, d-none, d-md-block
 * Neon třídy: bg-neon-black, text-neon-green
 * Vlastní: game-container, chat-container, app-container
 */

/**
 * SEZNAM PROMĚNNÝCH (globální a v metodách):
 * gameUI, chatUI, gameContainer, chatContainer, aiPlayerController, observer, viewportInfo, docHeight, appHeight, 
 * gameHeight, chatHeight, ratio, initTime, lastLayoutCheck, visibility
 * 
 * MOŽNÉ DUPLICITY: 
 * - Žádné duplicity nalezeny
 */

import { GameUI } from './js/ui/gameUI.js';